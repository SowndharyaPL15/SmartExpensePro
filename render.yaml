services:
  - type: web
    name: smartexpensepro-demo
    runtime: node
    rootDir: demo_live
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
    plan: free
